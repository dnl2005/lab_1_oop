<UserControl x:Class="UI.Views.TriggersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:ViewModel;assembly=ViewModel"
             Style="{StaticResource {x:Type UserControl}}"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.DataContext>
        <vm:TriggersViewModel />
    </UserControl.DataContext>


    <UserControl.Resources>
        <!-- Style Trigger -->
        <Style x:Key="TriggerButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Gray" />
            <Setter Property="Foreground" Value="White" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="Green" />
                </Trigger>
            </Style.Triggers>
        </Style>


        <!-- DataTrigger -->
        <Style x:Key="DataTriggerTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsActive}" Value="True">
                    <Setter Property="Foreground" Value="LightGreen" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>


    <StackPanel Margin="20">
        <TextBlock Text="Триггеры" FontSize="16" Margin="0,0,0,10" />


        <!-- Style Trigger -->
        <TextBlock Text="Style Trigger" FontWeight="Bold" />
        <Button Content="Наведи курсор"
                Style="{StaticResource TriggerButtonStyle}" Width="200" />


        <Separator Margin="10" />


        <!-- DataTrigger -->
        <TextBlock Text="DataTrigger" FontWeight="Bold" />
        <CheckBox Content="Активировать" IsChecked="{Binding IsActive}" />
        <TextBlock Text="Состояние активно"
                   Style="{StaticResource DataTriggerTextStyle}" />


        <Separator Margin="10" />


        <!-- EventTrigger -->
        <TextBlock Text="EventTrigger" FontWeight="Bold" />
        <Border Width="200" Height="50" Background="DarkGray">
            <Border.Triggers>
                <EventTrigger RoutedEvent="MouseEnter">
                    <BeginStoryboard>
                        <Storyboard>
                            <ColorAnimation Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                            To="Orange" Duration="0:0:0.3" />
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Border.Triggers>
        </Border>
    </StackPanel>
</UserControl>
