<UserControl x:Class="UI.Views.OneWayBindingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:ViewModel;assembly=ViewModel"
             Style="{StaticResource {x:Type UserControl}}"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.DataContext>
        <vm:OneWayBindingViewModel />
    </UserControl.DataContext>


    <StackPanel Margin="20">
        <TextBlock Text="Односторонние привязки" FontSize="16" Margin="0,0,0,10" />


        <!-- OneWay -->
        <TextBlock Text="OneWay (ViewModel → UI)" FontWeight="Bold" />
        <ProgressBar Height="20" Minimum="0" Maximum="100"
                     Value="{Binding ProgressValue, Mode=OneWay}" />
        <TextBlock Text="{Binding ProgressValue, StringFormat=Значение: {0}}" />
        <Button Content="Увеличить значение" Command="{Binding IncreaseCommand}" />


        <Separator Margin="10" />


        <!-- OneWayToSource -->
        <TextBlock Text="OneWayToSource (UI → ViewModel)" FontWeight="Bold" />
        <TextBox Width="200"
                 Text="{Binding InputText, Mode=OneWayToSource, UpdateSourceTrigger=PropertyChanged}" />
        <TextBlock Text="Значение во ViewModel:" />
        <TextBlock Text="{Binding InputText}" />
    </StackPanel>
</UserControl>
