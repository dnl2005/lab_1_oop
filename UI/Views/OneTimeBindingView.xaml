<UserControl x:Class="UI.Views.OneTimeBindingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:ViewModel;assembly=ViewModel"
             Style="{StaticResource {x:Type UserControl}}"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.DataContext>
        <vm:OneTimeBindingViewModel />
    </UserControl.DataContext>


    <StackPanel Margin="20">
        <TextBlock Text="Одноразовая привязка (OneTime)" FontSize="16" Margin="0,0,0,10" />


        <TextBlock Text="Привязка OneTime через ViewModel" FontWeight="Bold" />


        <!-- Значение читается один раз при инициализации -->
        <TextBlock Text="{Binding TimeStamp, Mode=OneTime}" FontSize="14" />


        <Button Content="Обновить значение во ViewModel"
                Command="{Binding UpdateTimeCommand}" />


        <Separator Margin="10" />


        <TextBlock Text="Для сравнения: OneWay" FontWeight="Bold" />
        <TextBlock Text="{Binding TimeStamp, Mode=OneWay}" FontSize="14" />
    </StackPanel>
</UserControl>
