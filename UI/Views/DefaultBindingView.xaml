<UserControl x:Class="UI.Views.DefaultBindingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:ViewModel;assembly=ViewModel"
             Style="{StaticResource {x:Type UserControl}}"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.DataContext>
        <vm:DefaultBindingViewModel />
    </UserControl.DataContext>


    <StackPanel Margin="20">
        <TextBlock Text="Привязка по умолчанию (TextBox -> TextBlock)"
                   FontSize="16" Margin="0,0,0,10" />


        <!-- Прямая привязка к элементу -->
        <TextBox x:Name="InputBox" Width="200" />
        <TextBlock Text="{Binding ElementName=InputBox, Path=Text}" Margin="0,5" />


        <Separator Margin="10" />


        <!-- Привязка через ViewModel -->
        <TextBox Width="200" Text="{Binding UserText}" />
        <TextBlock Text="{Binding UserText}" Margin="0,5" />
    </StackPanel>
</UserControl>
